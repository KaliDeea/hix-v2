{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Stores company information and verification status for businesses.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "format": "email" },
        "companyName": { "type": "string" },
        "vatNumber": { "type": "string" },
        "isVetted": { "type": "boolean" },
        "isVatVerified": { "type": "boolean" },
        "isSuspended": { "type": "boolean" },
        "suspensionReason": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin", "superadmin"] },
        "logoUrl": { "type": "string", "format": "uri" },
        "createdAt": { "type": "string", "format": "date-time" },
        "revenue": { "type": "number" },
        "commissionsPaid": { "type": "number" }
      },
      "required": ["uid", "email", "companyName", "role"]
    },
    "Report": {
      "title": "Report",
      "description": "User reports against other users for terms violations.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "reporterId": { "type": "string" },
        "reporterName": { "type": "string" },
        "reportedUserId": { "type": "string" },
        "reportedUserName": { "type": "string" },
        "transactionId": { "type": "string" },
        "listingId": { "type": "string" },
        "reason": { "type": "string" },
        "description": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "reviewed", "resolved", "dismissed"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["reporterId", "reportedUserId", "reason", "description"]
    },
    "Listing": {
      "title": "Listing",
      "description": "Industrial assets listed for sale.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "sellerId": { "type": "string" },
        "sellerName": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "price": { "type": "number" },
        "quantity": { "type": "number" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "co2Savings": { "type": "number" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "status": { "type": "string", "enum": ["available", "sold"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["sellerId", "title", "price", "quantity", "category"]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "Records of completed trades between buyers and sellers.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "listingId": { "type": "string" },
        "buyerId": { "type": "string" },
        "sellerId": { "type": "string" },
        "amount": { "type": "number" },
        "quantity": { "type": "number" },
        "buyerCommission": { "type": "number" },
        "sellerCommission": { "type": "number" },
        "co2Saved": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "completed", "cancelled"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "certificateUrl": { "type": "string" }
      },
      "required": ["listingId", "buyerId", "sellerId", "amount", "quantity"]
    },
    "HaulingQuoteRequest": {
      "title": "Hauling Quote Request",
      "description": "Requests for transport quotes from vetted haulers.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "haulerId": { "type": "string" },
        "haulerName": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "userEmail": { "type": "string" },
        "origin": { "type": "string" },
        "destination": { "type": "string" },
        "assetDetails": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "responded", "closed"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["haulerId", "userId", "origin", "destination", "assetDetails"]
    },
    "HaulingPartnerApplication": {
      "title": "Hauling Partner Application",
      "description": "Applications from hauling companies to become vetted partners.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "companyName": { "type": "string" },
        "contactEmail": { "type": "string" },
        "phoneNumber": { "type": "string" },
        "description": { "type": "string" },
        "fleetSize": { "type": "number" },
        "specializations": { "type": "array", "items": { "type": "string" } },
        "status": { "type": "string", "enum": ["pending", "reviewed", "approved", "rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["companyName", "contactEmail", "phoneNumber", "description"]
    },
    "Chat": {
      "title": "Chat",
      "description": "A conversation between two users regarding listings.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "participants": { "type": "array", "items": { "type": "string" }, "description": "UIDs of the two participants" },
        "participantNames": { "type": "object", "description": "Map of UID to company name" },
        "participantLogos": { "type": "object", "description": "Map of UID to logo URL" },
        "listingId": { "type": "string" },
        "listingTitle": { "type": "string" },
        "listingImage": { "type": "string" },
        "lastMessage": { "type": "string" },
        "lastMessageTime": { "type": "string", "format": "date-time" },
        "lastRead": { "type": "object", "description": "Map of UID to last read timestamp" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" },
        "isSupport": { "type": "boolean", "description": "Whether this is a support chat" },
        "supportMode": { "type": "string", "enum": ["ai", "agent"], "description": "Who is currently handling the support chat" }
      },
      "required": ["participants", "participantNames", "updatedAt", "createdAt"]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "description": "An individual message within a chat room.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "chatId": { "type": "string" },
        "senderId": { "type": "string" },
        "text": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["chatId", "senderId", "text", "createdAt"]
    },
    "AssetRequest": {
      "title": "Asset Request",
      "description": "Specific project requirements posted by buyers for AI matching.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "companyName": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "category": { "type": "string" },
        "budget": { "type": "number" },
        "deadline": { "type": "string", "format": "date-time" },
        "technicalSpecs": { "type": "string" },
        "quantity": { "type": "number" },
        "tags": { "type": "array", "items": { "type": "string" } },
        "status": { "type": "string", "enum": ["active", "matched", "closed"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "title", "description", "status"]
    },
    "Offer": {
      "title": "Offer",
      "description": "Counter-offers submitted by buyers for fixed-price listings.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "listingId": { "type": "string" },
        "buyerId": { "type": "string" },
        "buyerName": { "type": "string" },
        "sellerId": { "type": "string" },
        "amount": { "type": "number" },
        "quantity": { "type": "number" },
        "reason": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "accepted", "rejected", "countered"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["listingId", "buyerId", "sellerId", "amount", "quantity", "status"]
    },
    "WhitepaperRequest": {
      "title": "Whitepaper Request",
      "description": "Requests for the platform whitepaper to understand HIX's circular economy impact.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "company": { "type": "string" },
        "jobTitle": { "type": "string" },
        "interestReason": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "sent"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "name", "company", "status", "createdAt"]
    },
    "ErrorLog": {
      "title": "Error Log Entry",
      "description": "Schema for system error tracking",
      "type": "object",
      "properties": {
        "error": { "type": "string" },
        "operationType": { "type": "string" },
        "path": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "authInfo": { "type": "object" },
        "type": { "type": "string" },
        "url": { "type": "string" },
        "stack": { "type": "string" }
      },
      "required": ["error", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profile documents"
    },
    "/listings/{listingId}": {
      "schema": "Listing",
      "description": "Industrial asset listings"
    },
    "/transactions/{transactionId}": {
      "schema": "Transaction",
      "description": "Trade transaction records"
    },
    "/hauling_quotes/{quoteId}": {
      "schema": "HaulingQuoteRequest",
      "description": "Hauling quote requests"
    },
    "/hauling_applications/{applicationId}": {
      "schema": "HaulingPartnerApplication",
      "description": "Hauling partner applications"
    },
    "/chats/{chatId}": {
      "schema": "Chat",
      "description": "Chat rooms"
    },
    "/chats/{chatId}/messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "Individual messages in a chat"
    },
    "/reports/{reportId}": {
      "schema": "Report",
      "description": "User reports"
    },
    "/asset_requests/{requestId}": {
      "schema": "AssetRequest",
      "description": "Buyer asset requests"
    },
    "/offers/{offerId}": {
      "schema": "Offer",
      "description": "Buyer counter-offers"
    },
    "/whitepaper_requests/{requestId}": {
      "schema": "WhitepaperRequest",
      "description": "Platform whitepaper requests"
    },
    "/error_logs/{logId}": {
      "schema": "ErrorLog",
      "description": "System error reports"
    }
  }
}
